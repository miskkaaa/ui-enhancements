#pragma once

#include <Geode/Geode.hpp>
using namespace geode::prelude;

class UICreatorLayer {
public:
    //static void DoTheFunni(CreatorLayer* something);
    static void CreateTheThingies(CreatorLayer* something);
};